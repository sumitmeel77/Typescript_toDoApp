/// difference between ts and .tsx is that ts only include typescript where as .tsx allow typescript with jsx component also.

export interface Todo {
    id: number,
    todo: string,
    isDone: boolean
}